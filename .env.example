# Copy this file to .env in the repo root and fill in the value before running
# docker compose up. Compose reads .env from the same directory as the
# docker-compose*.yml files to substitute ${GEMINI_API_KEY}.
GEMINI_API_KEY=
